# R2.3 reward, study, and checkpoint verification — 2026-08-01

## Scope

This report covers the R2.3 substrate for durable verifier/reward evidence, versioned studies,
delayed retention and interference scheduling, and the registry lifecycle for externally produced
frozen checkpoints. It verifies software behavior and persistence; it is not an Inkling empirical
result and does not claim parameter learning.

No OpenAI or Anthropic request was made. No GCS object was read or written. No credential value was
loaded, copied, printed, hashed, or persisted. No checkpoint weights were changed, and the Magellan
repository was not accessed.

## Hermetic gate

`make check` passed:

- Ruff formatting: 148 files formatted;
- Ruff lint: passed;
- generated JSON Schema drift: 27 public contracts matched;
- strict mypy: 85 source files passed;
- local suite: 109 passed, 6 explicitly deselected external/platform gates.

The R2.3 integration cases specifically verified:

- immutable reward-policy registration and idempotent reward replay;
- deterministic recomputation from raw/normalized components, gate evidence, and policy digest;
- explicit missing values and the absence of scalar utility after hard-gate failure;
- training-lane exclusion plus independent eligibility-decision integrity verification;
- multi-experiment study aggregation with separate missing, contamination, and infrastructure
  attrition rather than imputation;
- retention snapshots taken from the source episode's final state;
- interference snapshots taken after a complete episode in a different competency;
- fresh rotating-shadow selection, coordinated trial/item leases, prompt-first exposure,
  completion, expiry recovery, and reclaim;
- checkpoint N/N+1 evaluation on one sealed suite, optional-metric missingness, lexicographic
  non-regression, promotion, revocation, comparison recomputation, and decision verification;
- read-only reward, study, and checkpoint reports with integrity results and no lease token output.

## Migration gate

Both migration integration tests passed. A separate empty SQLite database was upgraded through:

```text
08bc4fd6c08b -> 4a9c7d8e2f10 -> 0ace28c76900
```

`alembic check` then reported no new upgrade operations, and `alembic heads` reported only
`0ace28c76900`.

## Lean regression gate

Because `VerifierResult` gained a durable unique result identity, the real pinned Lean integration
test was rerun. Codex's outer sandbox cannot nest Padawan's required macOS sandbox, so the test was
executed outside only that outer restriction while retaining Padawan's own network-denying,
write-confined verifier sandbox. Kernel acceptance and rejection remained distinct; result:
`1 passed`.

## Honest boundaries

- Padawan now records and recomputes evidence and checkpoint governance; it still does not train a
  model or perform online/continual parameter updates.
- Retention and interference scheduling is operational as a durable service. Live target outcomes
  still depend on an operational Inkling endpoint.
- Algebra remains the only complete autonomous developmental workflow.
- Lean corpus generation and kernel verification are real; the full Lean teaching/transfer
  workflow remains R2.4 work.
- Magellan Improvement, the appellate briefing environment, and training-product compilation remain
  subsequent Round 2 tracks.
